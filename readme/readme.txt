goto pom.xml place
maven build
maven spring boot run
	mvn spring-boot:run
hit a url to crawl:  http://localhost:8080/crawl

To do
1. Create application cotext once
2. use a relative paths in application
3. Create relational database
4. create proper web pages
5. Check where it is getting datasouce from
    application.properties or spring.xml?
    